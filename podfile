# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LoveWish' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoveWish
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

  
  target 'LoveWishTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LoveWishUITests' do
    # Pods for testing
  end

end
